{{- if .Values.tenantReaderRole.create -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ .Values.tenantReaderRole.name }}
  labels:
    {{- include "namespace-bootstrap.labels" . | nindent 4 }}
  {{- with .Values.tenantReaderRole.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
rules:
- apiGroups: [""] # "" indicates the core API group
  resources:
  - bindings 
  - configmaps 
  - endpoints
  - events 
  - limitranges
  - persistentvolumeclaims
  - pods 
  - podtemplates
  - replicationcontrollers
  - resourcequotas 
  - secrets 
  - serviceaccounts
  - services
  - controllerrevisions
  - daemonsets
  - deployments
  - replicasets
  - statefulsets
  - localsubjectaccessreviews
  - horizontalpodautoscalers
  - cronjobs
  - jobs
  - leases 
  - endpointslices
  - events
  - pods 
  - ingresses
  - networkpolicies
  - poddisruptionbudgets 
  - rolebindings 
  - roles
  - volumesnapshots 
  verbs:
  - get
  - list
{{- end }}
