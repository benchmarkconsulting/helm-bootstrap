{{- if .Values.tenantAuditRole.create -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ .Values.tenantAuditRole.name }}
  labels:
    {{- include "namespace-bootstrap.labels" . | nindent 4 }}
  {{- with .Values.tenantAuditRole.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ .Values.tenantAuditRole.name }}
subjects:
- kind: Group
  namespace:  {{ .Values.nameSpace.name }}
  name: {{ .Values.tenantReaderRole.groupObjectId }}
{{- end }}
